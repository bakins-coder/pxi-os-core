# TASK: Redesign Xquisite Logo (Premium Refinement)

**To: Nova (Web Visualization Specialist)**
**From: Prof (Orchestrator)**

### Objective
Correct the Xquisite Celebrations logo in the sidebar. The user has provided feedback (and a screenshot) indicating it currently has a "glitchy" appearance with cyan/red offsets. We need to replace this with a premium, high-end, and clean aesthetic.

### Required Changes (Layout.tsx)
1.  **Remove Glitch**: Ensure all typography for "Xquisite Celebrations" is perfectly clean. No color offsets, shadows that look like chromatic aberration, or cramped tracking.
2.  **Premium Icon**: Refine the purple "X" box. Consider a subtle gradient (e.g., `bg-gradient-to-br from-indigo-600 to-indigo-700`) and a more professional `italic` font or a small SVG path if you think it fits better.
3.  **Typography Polish**: Use `tracking-widest` or `tracking-normal` instead of `tighter` for "Xquisite". Ensure the font weight (`font-black`) looks elegant on dark backgrounds.
4.  **Transitions**: Maintain smooth Hover transitions (`group-hover:rotate-6`).

### Resources
- [Layout.tsx](file:///c:/Users/akinb/pxi-os-core/src/components/Layout.tsx)
- [Implementation Plan](file:///c:/Users/akinb/.gemini/antigravity/brain/fffbd731-1236-45ec-bc73-d65e62faa18e/implementation_plan.md)

### Expected Output
- A code fix in `src/components/Layout.tsx`.
- A summary of the design decisions made.
- Verification that the logo is "Wow"-worthy.

---
**Status: PENDING APPROVAL** (Deliver once Akin approves the plan)
