# GitHub Routing Fix
If the GitHub icon still shows the old repository:
1. Refresh the browser tab.
2. The `package.json` update should trigger the IDE to notice the new identity.
3. Attempt to "Commit" again; it should now prompt for a new repository or sync with the updated path.